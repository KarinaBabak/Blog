@model IEnumerable<Blog.Models.TagViewModel>

@if(Model.Count()!= 0)
{
    foreach (var item in Model)
    {
        @Html.ActionLink(item.Name, "SearchByTag", new { controller = "Article", tagId = item.Id })
    }
}